; --finite-model-find --e-matching --proof-format-mode=alethe --proof-granularity=dsl-rewrite --full-saturate-quant --proof-mode=full-proof-strict --no-stats --sat-random-seed=1 --lang=smt2 --proof-prune --proof-prune-input --proof-elim-subtypes --proof-alethe-define-skolems --proof-format-mode=alethe --tlimit 30000
(set-option :produce-proofs true)
(set-logic AUFLIRA)
(declare-sort A$ 0)
(declare-sort Nat$ 0)
(declare-sort A_list$ 0)
(declare-sort A_a_fun$ 0)
(declare-sort A_list_list$ 0)
(declare-sort A_a_a_fun_fun$ 0)
(declare-fun a$ () A_list$)
(declare-fun b$ () A_list$)
(declare-fun f$ () A_a_a_fun_fun$)
(declare-fun i$ () Nat$)
(declare-fun j$ () Nat$)
(declare-fun n$ () A_list_list$)
(declare-fun v$ () A_list$)
(declare-fun ma$ () A_list_list$)
(declare-fun mat$ (Nat$ Nat$ A_list_list$) Bool)
(declare-fun nil$ () A_list$)
(declare-fun nth$ (A_list$ Nat$) A$)
(declare-fun cons$ (A_list$ A_list_list$) A_list_list$)
(declare-fun less$ (Nat$ Nat$) Bool)
(declare-fun nil$a () A_list_list$)
(declare-fun nth$a (A_list_list$ Nat$) A_list$)
(declare-fun size$ (A_list$) Nat$)
(declare-fun zero$ () Nat$)
(declare-fun size$a (A_list_list$) Nat$)
(declare-fun times$ (Nat$ Nat$) Nat$)
(declare-fun divide$ (Nat$ Nat$) Nat$)
(declare-fun modulo$ (Nat$ Nat$) Nat$)
(declare-fun fun_app$ (A_a_fun$ A$) A$)
(declare-fun fun_app$a (A_a_a_fun_fun$ A$) A_a_fun$)
(declare-fun row_length$ (A_list_list$) Nat$)
(declare-fun vec_mat_Tensor$ (A_a_a_fun_fun$ A_list$ A_list_list$) A_list_list$)
(assert (! (not (=> (and (and (less$ i$ (times$ (row_length$ (cons$ a$ (cons$ b$ n$))) (size$ v$))) (less$ j$ (size$a (cons$ a$ (cons$ b$ n$))))) (mat$ (row_length$ (cons$ a$ (cons$ b$ n$))) (size$a (cons$ a$ (cons$ b$ n$))) (cons$ a$ (cons$ b$ n$)))) (= (nth$ (nth$a (vec_mat_Tensor$ f$ v$ (cons$ a$ (cons$ b$ n$))) j$) i$) (fun_app$ (fun_app$a f$ (nth$ v$ (divide$ i$ (row_length$ (cons$ a$ (cons$ b$ n$)))))) (nth$ (nth$a (cons$ a$ (cons$ b$ n$)) j$) (modulo$ i$ (row_length$ (cons$ a$ (cons$ b$ n$))))))))) :named conjecture0))
(assert (! (or (= j$ zero$) (less$ zero$ j$)) :named a1))
(assert (! (= ma$ (cons$ b$ n$)) :named a2))
(assert (! (=> (and (= j$ zero$) (and (and (less$ i$ (times$ (row_length$ (cons$ a$ ma$)) (size$ v$))) (less$ j$ (size$a (cons$ a$ ma$)))) (mat$ (row_length$ (cons$ a$ ma$)) (size$a (cons$ a$ ma$)) (cons$ a$ ma$)))) (= (nth$ (nth$a (vec_mat_Tensor$ f$ v$ (cons$ a$ ma$)) j$) i$) (fun_app$ (fun_app$a f$ (nth$ v$ (divide$ i$ (row_length$ (cons$ a$ ma$))))) (nth$ (nth$a (cons$ a$ ma$) j$) (modulo$ i$ (row_length$ (cons$ a$ ma$))))))) :named a3))
(assert (! (=> (and (less$ zero$ j$) (and (and (less$ i$ (times$ (row_length$ (cons$ a$ (cons$ b$ n$))) (size$ v$))) (less$ j$ (size$a (cons$ a$ (cons$ b$ n$))))) (mat$ (row_length$ (cons$ a$ (cons$ b$ n$))) (size$a (cons$ a$ (cons$ b$ n$))) (cons$ a$ (cons$ b$ n$))))) (= (nth$ (nth$a (vec_mat_Tensor$ f$ v$ (cons$ a$ (cons$ b$ n$))) j$) i$) (fun_app$ (fun_app$a f$ (nth$ v$ (divide$ i$ (row_length$ (cons$ a$ (cons$ b$ n$)))))) (nth$ (nth$a (cons$ a$ (cons$ b$ n$)) j$) (modulo$ i$ (row_length$ (cons$ a$ (cons$ b$ n$)))))))) :named a4))
(assert (! (= (size$ nil$) zero$) :named a5))
(assert (! (= (size$a nil$a) zero$) :named a6))
(check-sat)
(get-proof)
